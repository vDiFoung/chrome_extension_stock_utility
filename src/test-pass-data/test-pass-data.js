// // Listen event from "background" file, background file dispatch an action, this file is called "content script"
// // it can listen chrome.runtime or chrome.tabs dispatch action from "background file"
// chrome.runtime.onMessage.addListener(function(request, messageSender) {
//     if (request.status) {
//
//     } else {
//
//     }
// })